# Task Manager API (refatorado)

API Flask de tarefas com estrutura MVC aprimorada pela skill `refactor-arch`.

```bash
pip install -r requirements.txt
python3 seed.py
python3 app.py
```

Health: `GET /health` · Tasks: `GET /tasks` · Categories: `GET /categories` · Login: `POST /login`
